{
  "name": "apheleia",
  "version": "1.0.0",
  "description": "fast and simplistic logger.",
  "main": "lib/Apheleia.js",
  "scripts": {
    "unit": "ava test/*Test.js",
    "test": "npm run lint && npm run unit",
    "lint": "standard DateFormatter.js lib/* test/* benchmark/*",
    "coveralls": "nyc --reporter=text-lcov npm test",
    "coverage": "nyc npm test && nyc report --reporter=html",
    "benchmark": "node benchmark/benchmarkSimpleFormat.js && node benchmark/benchmarkJsonFormat.js && node benchmark/benchmarkChildLogging.js && node benchmark/benchmarkChildChildLogging.js && node benchmark/benchmarkChildCreation.js && node benchmark/benchmarkObjectJsonFormat.js"
  },
  "author": "Denis Fäcke",
  "license": "MIT",
  "dependencies": {
    "fast-date-format": "^2.2.2",
    "json-escaping": "^1.0.1"
  },
  "engines": {
    "node": ">= 8.0.0"
  },
  "devDependencies": {
    "ava": "^2.0.0",
    "coveralls": "^3.0.2",
    "fastbench": "^1.0.1",
    "fastify": "^2.3.0",
    "nyc": "^14.1.0",
    "pino": "^5.12.2",
    "request": "^2.88.0",
    "sonic-boom": "^0.7.3",
    "standard": "^14.0.0",
    "winston": "^3.1.0"
  },
  "keywords": [
    "logger",
    "logging",
    "logs",
    "stream",
    "json",
    "fast"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SerayaEryn/apheleia.git"
  },
  "types": "./types.d.ts"
}
